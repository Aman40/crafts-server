/**
 * This handles requests specific to the providers' page/mobile app
 */
const express = require('express');
const router = express.Router();
const mysql = require('mysql');

module.exports = router;